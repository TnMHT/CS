
// LINEAR SEARCH
// finding a target value within a list using traversal
// worst case is 0(n)

const names = ["tom", "jerry", "dre", "james", "karen", "lisa"];

names.indexOf("lisa");

names.findIndex((item) => item === "james");

names.find((item) => item === "jerry");

names.includes("tom");

// BINARY SEARCH
